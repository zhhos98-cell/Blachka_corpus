# Archived bibliography build passes

Archived on 2026-08-17 during repository housekeeping.

This directory preserves the historical bibliography pass scripts formerly stored under `bibliography/legacy/passes/`. The relocation was lossless: the original Git blob objects were reused, so file contents and blob SHAs are unchanged.

The active bibliography does not depend on these pass scripts. They are retained only as build provenance and rollback/history material.

Archived range: `bibliography-pass6.js` through `bibliography-pass37.js` (32 files).
