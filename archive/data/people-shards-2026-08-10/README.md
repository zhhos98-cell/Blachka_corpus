# Historical People shards · generated 10 August 2026

This directory preserves the retired `people-part-01.json` through `people-part-17.json` files and their `people-records.json` manifest exactly as they existed before the public-data cleanup.

They are not canonical inputs. The old manifest declares 162 records, while the current canonical authority file `people/people-data.json` declares and contains 153 records. That mismatch is preserved here as provenance rather than silently corrected in the historical files.

Current use:

- canonical research authority data: `people/people-data.json`
- derived public payload: `people/people-ui.json`
- reproducible projection: `scripts/build-people-ui.py`
